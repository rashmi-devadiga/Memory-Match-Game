// main.js
// Application entry point
// Ensures all scripts are loaded and the DOM is ready

document.addEventListener("DOMContentLoaded", () => {
  console.log("🎮 Memory Card Game initialized successfully");
});
